# Trace — Python dependencies (pinned for reproducible builds).
# c2pa-python is pinned per the report's supply-chain mitigation (Sec 26.2):
# "Pin to specific version; review diff on update; do not auto-update."
c2pa-python==0.37.10
cryptography>=41.0.0,<51.0.0

# --- Dev / test ---
pytest>=8.0.0,<9.0.0
pytest-cov>=5.0.0,<7.0.0
